import React, { Component } from 'react'
import { ScrollView } from 'react-native'

class App extends Component {
  render() {
    return (
      <ScrollView>

      </ScrollView>
    )
  }
}

export default App